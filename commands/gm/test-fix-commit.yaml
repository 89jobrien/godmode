name: test-fix-commit
description: "Run workspace tests, fix failures, and commit the verified changes."
template: dev
prompt: Run `cargo nextest run --workspace`, fix any failures, and commit fixes
allowedTools:
  - Edit
  - Read
  - Bash
  - Write
maxTurns: 20
